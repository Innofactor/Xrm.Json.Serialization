version: 1.0.{build}
configuration: Release
platform: Any CPU
before_build:
- cmd: nuget restore
build:
  parallel: true
  verbosity: minimal
after_build:
- cmd: nuget pack -Version %APPVEYOR_BUILD_VERSION%
artifacts:
- path: '*.nupkg'